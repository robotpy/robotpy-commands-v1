---

classes:
  PIDAnalogInput:
    subpackage: command
    methods:
      PIDGet:
    
    force_no_default_constructor: true

inline_code: |
  cls_PIDAnalogInput
    .def(py::init<int>(), release_gil());