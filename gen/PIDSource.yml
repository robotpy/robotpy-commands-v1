---

enums:
  PIDSourceType:
classes:
  PIDSource:
    shared_ptr: true
    subpackage: command
    attributes:
      m_pidSource:
    methods:
      SetPIDSourceType:
      GetPIDSourceType:
      PIDGet:
        rename: pidGet
